<UserControl x:Class="intf.Views.ConfirmationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:intf.Views"
             mc:Ignorable="d" 
             d:DesignWidth="600"
             Background="#FFF">

    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="{Binding Text}" FontSize="19" />
    
            <StackPanel Margin="0 35 0 0" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="YES" Command="{Binding YesCommand}" Width="200" Margin="0 0 10 0" Style="{StaticResource PrimaryButton}" />
                <Button Content="NO" Command="{Binding CancelCommand}" Width="200" Style="{StaticResource DefaultButton}" />
            </StackPanel>
    
        </StackPanel>
    </Border>

</UserControl>
